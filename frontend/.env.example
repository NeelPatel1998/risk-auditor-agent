# Leave unset during local dev to use the Vite /api proxy (recommended).
# VITE_API_URL=http://127.0.0.1:8000

# Optional: max milliseconds to wait for one streaming chat reply (default 420000 = 7 minutes).
# VITE_CHAT_STREAM_TIMEOUT_MS=600000
